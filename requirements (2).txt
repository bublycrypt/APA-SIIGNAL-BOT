pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0
# Optional but recommended:
# smartmoneyconcepts   # if you want to lean on the popular library later
# ccxt                 # for live exchange data
# MetaTrader5          # if targeting MT5 / Deriv
